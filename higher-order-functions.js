/**
 * forEach:
 * arr.forEach((callbackfunc(value at each index, index, arr)))
 * A shorthand for loop that doesnt return anyhthing
 */

/**
 * filter: 
 * arr.filter(callbackfunc(val) { if (){ return true;})
 * This used to filter all the elements that matches the condition
 */

/**
 * map:
 * Returns a new array after looping through each elements in the array and performing an operation
 * arr.map(callbacfunc(elem) {return new arr})
 */

/**
 * Sort: 
 * Sort the companies based on lexicographically increasing order
 * We can add a comparator function
 */

/**
 * reduce arr: 
 * arr.reduce(function(total, val){}, 0)
 */